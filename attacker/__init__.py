"""Attacker model rebuilt on dense video tracks + FIFA EFI labels (novelty C3, C4).

Repairs the freeze-frame-era attacker failures (run head ~6.33 m vs 6.46 m no-move; receiver tied
with a nearest-teammate heuristic) using persistent IDs, body orientation and per-player EFI labels.
"""
